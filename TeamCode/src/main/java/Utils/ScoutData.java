package Utils;

import MathSystems.Angle;

public class ScoutData {
    public static boolean dataStale = true;
    public static Angle turretAngle = Angle.ZERO(), pitchAngle = Angle.ZERO();
    public static int extensionDistance = 0;
}
